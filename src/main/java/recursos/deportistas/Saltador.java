package recursos.deportistas;

/**
 * y por último otra interfaz llamada Saltador con un método saltarAltura que no
 * recibe parámetros y que devuelve los centímetros saltados.
 */

public interface Saltador {
    abstract int saltarAltura();
}
